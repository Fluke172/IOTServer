server:
  # ????HTTP??????80
  port: 8081
  servlet:
    # ???????
    context-path: /
  tomcat:
    # tomcat?URI??
    uri-encoding: UTF-8
    # ?????????????100
    accept-count: 1000
    threads:
      # tomcat?????????200
      max: 800
      # Tomcat?????????????10
      min-spare: 100


spring:
  application:
    name: station-gateway
  servlet:
    multipart:
      enabled: false
  mail:
    username: 1228231744
    password: eyoyztteddujbagc
    host: smtp.qq.com
    properties:
        mail:
          smtp:
            ssl:
              enable: true
  redis:
    port: 6379
    host: 127.0.0.1
    password:

  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/iotdb?serverTimezone=UTC&userUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

    druid:
      #druid ???????
      initialSize: 5
      minIdle: 5
      maxActive: 20
      maxWait: 60000
      timeBetweenEvictionRunsMillis: 60000
      minEvictableIdleTimeMillis: 300000
      validationQuery: SELECT 1 FROM DUAL
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      poolPreparedStatements: true

      #?????????filters?stat:?????log4j??????wall???sql??
      #???????  java.lang.ClassNotFoundException: org.apache.log4j.Priority
      #??? log4j ?????Maven ???https://mvnrepository.com/artifact/log4j/log4j
      filters: stat,wall,log4j
      maxPoolPreparedStatementPerConnectionSize: 20
      useGlobalDataSourceStat: true
      connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500

mybatis:
  #??pojo??????mybatis?????????pojo??
  type-aliases-package: com.cssf.pojo
  #??????Mapper?????XML?? classpath:xml????
  mapper-locations: classpath:mapper/*.xml
  configuration-properties:
    helperDialect: mysql
    offsetAsPageNum: true
    rowBoundsWithCount: true
    reasonable: true
netty:
  server:
    port: 8080
